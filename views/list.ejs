<%- include("header") -%> 
    <h1> <%= eday %>   </h1>
 <div class="mainListDiv">
     <div class="list">
                <% enewlist.forEach(element => { %>
             <div class="listele">  <input type="checkbox"> <p> <%= element.name %> </p> </div> 
                <% }) %>

        </div>
    <div class="btnandtext">
        <form action="/" method="POST">
            <input type="text" name="etx" class="ntext" placeholder="Type Here" autocomplete="off">
            <button class="btn" type="submit" name="subName" value="<%= eday %>" >+</button>
        </form>
    </div>
</div>
<%- include("footer") -%> 